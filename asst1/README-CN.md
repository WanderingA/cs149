# 作业1：四核CPU上的性能分析 #

**截止日期：10月7日星期一，晚上11:59**

**总分100分 + 额外6分**

## 概述 ##

这个作业旨在帮助你理解现代多核CPU中存在的两种主要并行执行形式：

1. 单个处理核心内的SIMD执行
2. 使用多个核心的并行执行（你还会看到Intel超线程的效果。）

你还将获得测量和推理并行程序性能的经验（这是一个具有挑战性但重要的技能，你将在整个课程中使用）。这个作业涉及的编程量很少，但分析量很大！

## 环境设置 ##

__你需要在新的myth机器上运行代码来完成这个作业__
（这些机器的主机名是`myth[51-66].stanford.edu`）。如果由于某种原因你在myth机器上没有主目录，请在[这里](https://stanford.service-now.com/it_services?id=sc_cat_item&sys_id=cab169801bd918d0685d4377cc4bcbe0)提交HelpSU工单。

这些机器包含四核4.2 GHz的Intel Core i7处理器（虽然动态频率缩放可以在芯片认为有用且可能的情况下将其提升到4.5 GHz）。处理器中的每个核心支持两个硬件线程（Intel称之为“超线程”），并且核心可以执行AVX2矢量指令，这些指令描述了在多个单精度数据值上同时执行相同的八宽操作。对于好奇的人，可以在<https://ark.intel.com/products/97129/Intel-Core-i7-7700K-Processor-8M-Cache-up-to-4-50-GHz->找到该CPU的完整规格。想要深入了解的学生可能会喜欢[这篇文章](https://en.wikichip.org/wiki/intel/microarchitectures/kaby_lake)。

注意：为了评分目的，我们希望你报告在Stanford myth机器上运行代码的性能，但为了好玩，你也可以在自己的机器上运行这个作业中的程序。（你首先需要安装Intel SPMD程序编译器（ISPC），可以在这里<http://ispc.github.io/>下载）。如果你在报告中包含了在其他机器上运行代码的发现，请务必明确说明你运行的机器。

开始：

1. ISPC是编译本作业中使用的许多程序所需的。ISPC可以通过以下步骤轻松安装在myth机器上：

从myth机器上，将Linux二进制文件下载到你选择的本地目录。你可以从ISPC[下载页面](https://ispc.github.io/downloads.html)获取Linux的ISPC编译器二进制文件。从`myth`，我们建议你使用`wget`直接从下载页面下载二进制文件。截至2024年秋季第1周，以下`wget`命令行有效：

    wget https://github.com/ispc/ispc/releases/download/v1.24.0/ispc-v1.24.0-linux.tar.gz

解压下载的文件：`tar -xvf ispc-v1.24.0-linux.tar.gz`

将ISPC的`bin`目录添加到你的系统路径中。例如，如果解压下载的文件生成了目录`~/Downloads/ispc-v1.24.0-linux`，在bash中你可以通过以下命令更新你的路径变量：

    export PATH=$PATH:${HOME}/Downloads/ispc-v1.24.0-linux/bin

可以将上述行添加到你的`.bashrc`文件中以永久生效。

如果你使用csh，你将使用`setenv`更新你的`PATH`。快速的Google搜索将教你如何操作。

2. 作业的起始代码可以在<https://github.com/stanford-cs149/asst1>找到。请使用以下命令克隆作业1的起始代码：

    `git clone https://github.com/stanford-cs149/asst1.git`

## 程序1：使用线程的并行分形生成（20分） ##

在代码库的`prog1_mandelbrot_threads/`目录中构建并运行代码。（输入`make`进行构建，输入`./mandelbrot`运行它。）该程序生成图像文件`mandelbrot-serial.ppm`，这是一个著名的复数集合Mandelbrot集合的可视化。大多数平台都有.ppm查看器。要远程查看生成的图像，首先确保你有_X服务器_。Linux系统不需要下载任何东西。然而，对于Mac，你可以使用[Xquartz](https://www.xquartz.org/)，对于Windows，你可以使用[VcXsrv](https://sourceforge.net/projects/vcxsrv/)。在你有SSH X转发支持后，确保你使用`ssh -Y`登录到myth机器，然后你可以使用`display`命令查看图像。正如你在下面的图像中所看到的，结果是一个熟悉而美丽的分形。图像中的每个像素对应于复平面中的一个值，每个像素的亮度与确定该值是否包含在Mandelbrot集合中的计算成本成正比。要获得图像2，请使用命令选项`--view 2`。（参见`mandelbrotSerial.cpp`中定义的函数`mandelbrotSerial()`）。你可以在<http://en.wikipedia.org/wiki/Mandelbrot_set>了解更多关于Mandelbrot集合的定义。

![Mandelbrot Set](handout-images/mandelbrot_viz.jpg "Mandelbrot集合的两个视图的可视化。计算每个像素的成本与其亮度成正比。在运行程序1和3时，你可以使用命令行选项`--view 2`将输出设置为视图2。")

你的任务是使用[std::thread](https://en.cppreference.com/w/cpp/thread/thread)并行化图像的计算。函数`mandelbrotThread()`中提供了生成一个额外线程的起始代码。在这个函数中，主应用程序线程使用构造函数`std::thread(function, args...)`创建另一个额外线程。它通过调用线程对象上的`join`等待该线程完成。目前，启动的线程不进行任何计算并立即返回。你应该在`workerThreadStart`函数中添加代码来完成这个任务。你不需要在这个作业中使用任何其他std::thread API调用。

**你需要做的事情：**

1. 修改起始代码，使用两个处理器并行化Mandelbrot生成。具体来说，在线程0中计算图像的上半部分，在线程1中计算图像的下半部分。这种问题分解称为_空间分解_，因为图像的不同空间区域由不同的处理器计算。
2. 扩展你的代码以使用2、3、4、5、6、7和8个线程，相应地划分图像生成工作（线程应获得图像的块）。注意，处理器只有四个核心，但每个核心支持两个超线程，因此它可以在其执行内容上交错执行总共八个线程。在你的报告中，生成一个__相对于参考顺序实现的加速图__，作为使用线程数的函数__仅针对视图1__。加速是否与使用的线程数线性相关？在你的报告中假设为什么是（或不是）这种情况？（你可能还希望生成视图2的图表，以帮助你提出一个好的答案。提示：仔细查看三线程的数据点。）
3. 为了确认（或否定）你的假设，通过在`workerThreadStart()`的开始和结束插入计时代码来测量每个线程完成其工作的时间。你的测量结果如何解释你之前创建的加速图？
4. 修改工作到线程的映射，以提高视图1和视图2的Mandelbrot集合的加速到__大约7-8倍__（如果超过7倍，那也没关系，不用担心）。你不能在你的解决方案中使用任何线程之间的同步。我们期望你提出一个适用于所有线程数的单一工作分解策略——不允许硬编码特定于每个配置的解决方案！（提示：有一个非常简单的静态分配可以实现这个目标，并且不需要线程之间的通信/同步）。在你的报告中，描述你的并行化方法并报告最终获得的8线程加速。
5. 现在用16个线程运行你的改进代码。性能是否明显高于使用八个线程时？为什么或为什么不？

## 程序2：使用SIMD内在函数矢量化代码（20分） ##

查看作业1代码库中`prog2_vecintrin/main.cpp`中的函数`clampedExpSerial`。`clampedExp()`函数将`values[i]`提升到`exponents[i]`给定的幂次，并将结果值限制在9.999999。在程序2中，你的任务是矢量化这段代码，使其可以在具有SIMD矢量指令的机器上运行。

然而，与其使用映射到现代CPU上实际SIMD矢量指令的SSE或AVX2矢量内在函数来实现，不如使用CS149的“伪矢量内在函数”定义在`CS149intrin.h`中来实现。`CS149intrin.h`库为你提供了一组操作矢量值和/或矢量掩码的矢量指令。（这些函数不会转换为实际的CPU矢量指令，而是我们在库中模拟这些操作，并提供便于调试的反馈。）作为使用CS149内在函数的示例，`main.cpp`中给出了矢量化的`abs()`函数版本。这个示例包含了一些基本的矢量加载和存储以及掩码寄存器的操作。注意，`abs()`示例只是一个简单的示例，实际上代码并不能正确处理所有输入！（我们会让你找出原因！）你可能希望阅读`CS149intrin.h`中的所有注释和函数定义，以了解可用的操作。

以下是一些帮助你实现的提示：

- 每个矢量指令都受可选的掩码参数的约束。掩码参数定义了哪些通道的输出在此操作中被“掩码”。掩码中的0表示通道被掩码，因此其值不会被矢量操作的结果覆盖。如果操作中未指定掩码，则没有通道被掩码。（注意，这相当于提供了一个全为1的掩码。）
  *提示：*你的解决方案需要使用多个掩码寄存器和库中提供的各种掩码操作。
- *提示：* 使用`_cs149_cntbits`函数在这个问题中很有帮助。
- 考虑如果总循环迭代次数不是SIMD矢量宽度的倍数会发生什么。我们建议你使用`./myexp -s 3`测试你的代码。*提示：*你可能会发现`_cs149_init_ones`很有帮助。
- *提示：* 使用`./myexp -l`在程序结束时打印执行的矢量指令日志。使用`addUserLog()`函数在日志中添加自定义调试信息。随意添加额外的`CS149Logger.printLog()`来帮助你调试。

程序的输出将告诉你你的实现是否生成了正确的输出。如果有不正确的结果，程序将打印出它找到的第一个错误，并打印出函数输入和输出的表格。你的函数输出在“output = ”之后，应该与“gold = ”之后的结果匹配。程序还会打印出描述CS149伪矢量单元利用率的统计信息。你应该将你的实现性能视为“总矢量指令”的值。（你可以假设每个CS149伪矢量指令在CS149伪SIMD CPU上需要一个周期。）“矢量利用率”显示启用的矢量通道的百分比。

**你需要做的事情：**

1. 在`clampedExpVector`中实现`clampedExpSerial`的矢量化版本。你的实现应适用于任何输入数组大小（`N`）和矢量宽度（`VECTOR_WIDTH`）的组合。
2. 运行`./myexp -s 10000`并扫过矢量宽度从2、4、8到16。记录结果的矢量利用率。你可以通过更改`CS149intrin.h`中的`#define VECTOR_WIDTH`值来实现这一点。随着`VECTOR_WIDTH`的变化，矢量利用率是增加、减少还是保持不变？为什么？
3. _额外加分：（1分）_ 在`arraySumVector`中实现`arraySumSerial`的矢量化版本。你的实现可以假设`VECTOR_WIDTH`是输入数组大小`N`的因子。虽然顺序实现运行时间为`O(N)`，你的实现应目标为`(N / VECTOR_WIDTH + VECTOR_WIDTH)`甚至`(N / VECTOR_WIDTH + log2(VECTOR_WIDTH))`。你可能会发现`hadd`和`interleave`操作有用。

### 程序3，第1部分。ISPC基础知识（20分中的10分）###

在阅读ISPC代码时，你必须记住，尽管代码看起来很像C/C++代码，但ISPC的执行模型与标准C/C++不同。与C不同，ISPC程序的多个程序实例总是在CPU的SIMD执行单元上并行执行。由编译器（并专门为底层机器选择）确定同时执行的程序实例数量。这个并发实例的数量可以通过内置变量`programCount`获得。ISPC代码可以通过内置的`programIndex`引用其自己的程序实例标识符。因此，从C代码调用ISPC函数可以被认为是生成了一组并发的ISPC程序实例（在ISPC文档中称为gang）。这组实例运行到完成，然后控制返回到调用的C代码。

__停下。这是你友好的讲师。请再次阅读前面的段落。相信我。__

例如，以下程序使用常规C代码和ISPC代码的组合来添加两个1024元素的向量。正如我们在课堂上讨论的那样，由于gang中的每个实例是独立的并且执行完全相同的程序逻辑，因此可以通过使用SIMD指令来加速执行。

下面是一个简单的ISPC程序。以下C代码将调用以下ISPC代码：

    ------------------------------------------------------------------------
    C程序代码：myprogram.cpp
    ------------------------------------------------------------------------
    const int TOTAL_VALUES = 1024;
    float a[TOTAL_VALUES];
    float b[TOTAL_VALUES];
    float c[TOTAL_VALUES];
 
    // 在这里初始化数组a和b。
     
    sum(TOTAL_VALUES, a, b, c);
 
    // 从sum返回后，a + b的结果存储在c中。

对应的ISPC代码：

    ------------------------------------------------------------------------
    ISPC代码：myprogram.ispc
    ------------------------------------------------------------------------
    export sum(uniform int N, uniform float* a, uniform float* b, uniform float* c)
    {
      // 假设programCount可以整除N。
      for (int i=0; i<N; i+=programCount)
      {
        c[programIndex + i] = a[programIndex + i] + b[programIndex + i];
      }
    }

上面的ISPC程序代码在程序实例之间交错处理数组元素。注意与程序1的相似之处，在程序1中你静态地将图像的部分分配给线程。

然而，与其考虑如何在程序实例之间划分工作（即，如何将工作映射到执行单元），通常更方便且更强大的是仅关注将问题划分为独立部分。ISPC的`foreach`构造提供了一种表达问题分解的机制。下面，ISPC函数`sum2`中的`foreach`循环定义了一个迭代空间，其中所有迭代都是独立的，因此可以以任何顺序执行。ISPC处理将循环迭代分配给并发程序实例。下面的`sum`和`sum2`之间的区别是微妙的，但非常重要。`sum`是命令式的：它描述了如何将工作映射到并发实例。下面的示例是声明式的：它仅指定要执行的工作集。

    -------------------------------------------------------------------------
    ISPC代码：
    -------------------------------------------------------------------------
    export sum2(uniform int N, uniform float* a, uniform float* b, uniform float* c)
    {
      foreach (i = 0 ... N)
      {
        c[i] = a[i] + b[i];
      }
    }

在继续之前，建议你通过阅读ISPC演练来熟悉ISPC语言结构，该演练可在<http://ispc.github.io/example.html>找到。演练中的示例程序几乎与程序3的`mandelbrot_ispc()`在`mandelbrot.ispc`中的实现完全相同。在作业代码中，我们更改了`foreach`循环的边界，以实现更直接的实现。

**你需要做的事情：**

1. 编译并运行程序mandelbrot ispc。__ISPC编译器当前配置为发出8宽AVX2矢量指令。__ 根据你对这些CPU的了解，你期望的最大加速是多少？为什么你观察到的数字可能低于这个理想值？（提示：考虑你正在执行的计算的特性？描述图像的哪些部分对SIMD执行提出了挑战？比较渲染Mandelbrot集的不同视图的性能可能有助于确认你的假设。）

我们提醒你，对于本节中描述的代码，ISPC编译器将程序实例的gang映射到单核上执行的SIMD指令。这种并行化方案与程序1不同，在程序1中，通过在多个核心上运行线程来实现加速。

如果你查看关于myth机器中CPU的详细技术资料，你会发现关于每个时钟周期可以运行多少标量和矢量指令有一套复杂的规则。对于本作业，你可以假设浮点数学的标量执行单元和8宽矢量执行单元的数量大致相同。

### 程序3，第2部分：ISPC任务（20分中的10分）###

ISPC的SPMD执行模型和`foreach`等机制促进了利用SIMD处理的程序的创建。该语言还提供了一种利用多个核心进行ISPC计算的附加机制。这种机制是启动_ISPC任务_。

参见函数`mandelbrot_ispc_withtasks`中的`launch[2]`命令。此命令启动两个任务。每个任务定义一个将由一组ISPC程序实例执行的计算。正如函数`mandelbrot_ispc_task`所示，每个任务计算最终图像的一个区域。类似于`foreach`构造定义了可以以任何顺序（并且由ISPC程序实例并行）执行的循环迭代，启动操作创建的任务可以以任何顺序（并且在不同的CPU核心上并行）处理。

**你需要做的事情：**

1. 使用参数`--tasks`运行`mandelbrot_ispc`。你在视图1上观察到的加速是多少？与不将计算划分为任务的`mandelbrot_ispc`版本相比，加速是多少？
2. 通过更改代码创建的任务数量，有一种简单的方法可以提高`mandelbrot_ispc --tasks`的性能。仅通过更改函数`mandelbrot_ispc_withtasks()`中的代码，你应该能够实现超过顺序版本代码32倍的性能。你是如何确定要创建的任务数量的？为什么你选择的数量效果最好？
3. _额外加分：（2分）_ 线程抽象（在程序1中使用）和ISPC任务抽象之间有什么区别？（创建/加入）和（启动/同步）机制之间在语义上的一些明显区别，但这些区别的影响更为微妙。这里有一个思维实验来指导你的答案：当你启动10,000个ISPC任务时会发生什么？当你启动10,000个线程时会发生什么？（对于这个思维实验，请在一般情况下讨论——即不要将你的讨论与给定的Mandelbrot程序联系起来。）

_聪明学生的问题_：嘿，等等！为什么ISPC系统有两种不同的机制（`foreach`和`launch`）来表达独立的、可并行化的工作？系统不能只将`foreach`的许多迭代划分到所有核心上并发出适当的SIMD代码吗？

_答案_：好问题！有很多可能的答案。来办公室时间讨论吧。

## 程序4：迭代`sqrt`（15分）##

程序4是一个ISPC程序，它计算2000万个0到3之间的随机数的平方根。它使用牛顿法的快速迭代实现来求解方程${\frac{1}{x^2}} - S = 0$。在此实现中，使用1.0作为初始猜测值。下图显示了`sqrt`在（0-3）范围内收敛到准确解所需的迭代次数。（对于此范围之外的输入，实施不会收敛）。请注意，收敛速度取决于初始猜测的准确性。

注意：这个问题是一个复习，以双重检查你的理解，因为它涵盖了与程序2和3相似的概念。

![sqrt收敛](handout-images/sqrt_graph.jpg "在0-3范围内以1.0为初始猜测的sqrt收敛。注意，对于输入值为1时，收敛是立即的，而当输入值接近0或3时，迭代次数增加（最高值为输入值为3时）。")

**你需要做的事情：**

1. 构建并运行`sqrt`。报告单个CPU核心（无任务）和使用所有核心（有任务）时的ISPC实现加速。SIMD并行化带来的加速是多少？多核并行化带来的加速是多少？
2. 修改数组values的内容，以提高ISPC实现的相对加速。构造一个特定的输入，__最大化相对于顺序版本代码的加速__，并报告所实现的加速（对于有任务和无任务的ISPC实现）。你的修改是否提高了SIMD加速？它是否提高了多核加速（即，从无任务的ISPC到有任务的ISPC的好处）？请解释原因。
3. 为`sqrt`构造一个特定的输入，__最小化ISPC（无任务）相对于顺序版本代码的加速__。描述这个输入，描述你选择它的原因，并报告ISPC实现的相对性能。效率损失的原因是什么？（请记住，我们使用`--target=avx2`选项为ISPC生成8宽SIMD指令）。
4. _额外加分：（最多2分）_ 使用AVX2内在函数手动编写`sqrt`函数的版本。要获得加分，你的实现应该几乎与使用ISPC生成的二进制文件一样快（或更快）。你可能会发现[Intel Intrinsics Guide](https://software.intel.com/sites/landingpage/IntrinsicsGuide/)非常有帮助。

## 程序5：BLAS `saxpy`（10分）##

程序5是BLAS（基本线性代数子程序）库中`saxpy`例程的实现，该库在许多系统上广泛使用（并经过高度优化）。`saxpy`计算简单操作`result = scale*X+Y`，其中`X`、`Y`和`result`是`N`个元素的向量（在程序5中，`N` = 2000万），`scale`是一个标量。注意，`saxpy`每使用三个元素执行两个数学操作（一个乘法，一个加法）。`saxpy`是一个*可平行化的计算*，具有可预测的、规则的数据访问和可预测的执行成本。

**你需要做的事情：**

1. 编译并运行`saxpy`。程序将报告`saxpy`的ISPC（无任务）和ISPC（有任务）实现的性能。你观察到的使用任务的ISPC加速是多少？解释该程序的性能。你认为它可以大幅改进吗？（例如，你能重写代码以实现接近线性加速吗？是或否？请解释你的答案。）
2. __额外加分：__（1分）注意，`main.cpp`中的总内存带宽消耗计算为`TOTAL_BYTES = 4 * N * sizeof(float);`。尽管`saxpy`从X加载一个元素，从Y加载一个元素，并将一个元素写入`result`，但乘以4是正确的。为什么会这样？（提示，考虑CPU缓存的工作原理。）
3. __额外加分：__（根据具体情况处理）提高`saxpy`的性能。我们在寻找显著的加速，而不仅仅是几个百分点。如果成功，请描述你是如何做到的，以及在这些系统上可能实现的最佳实现是什么。如果成功，请告诉工作人员，我们会感兴趣。;-)

注意：一些学生在这个问题上被困住了（想得太多）。我们期望一个简单的答案，但运行这个问题的结果可能会引发你脑中的更多问题。欢迎来与工作人员讨论。

## 程序6：加速`K-Means`（15分）##

程序6使用K-Means数据聚类算法（[维基百科](https://en.wikipedia.org/wiki/K-means_clustering)，[CS 221讲义](https://stanford.edu/~cpiech/cs221/handouts/kmeans.html)）对一百万个数据点进行聚类。如果你不熟悉该算法，不用担心！具体细节对练习并不重要，但在高层次上，给定K个起始点（聚类中心），该算法迭代更新中心，直到满足收敛标准。下图显示了程序开始和结束时算法的状态，其中红色星星是聚类中心，数据点颜色对应于聚类分配。

![K-Means起始和结束点](./handout-images/kmeans.jpg "K-Means算法应用于二维数据的起始和结束点。")

在起始代码中，你已经得到了K-means算法的正确实现，但在其当前状态下，它的速度还不够快。这就是你的任务！你的任务是找出实现需要改进的地方以及如何改进。这个问题的关键技能是__隔离性能热点__。我们不会告诉你在哪里查找代码。你需要自己找出来。你的第一个想法应该是……代码在哪里花费了最多的时间，你应该在源代码中插入计时代码进行测量。根据这些测量结果，你应该专注于占用大量运行时间的代码部分，然后更仔细地理解它，以确定是否有方法可以加速它。

**你需要做的事情：**

1. 使用命令`ln -s /afs/ir.stanford.edu/class/cs149/data/data.dat ./data.dat`在当前目录中创建一个指向数据集的符号链接（确保你在`prog6_kmeans`目录中）。这是一个大文件（约800MB），因此这是访问它的首选方式。然而，如果你想要一个本地副本，你可以在个人机器上运行此命令`scp [Your SUNetID]@myth[51-66].stanford.edu:/afs/ir.stanford.edu/class/cs149/data/data.dat ./data.dat`。一旦你有了数据，编译并运行`kmeans`（第一次尝试加载数据时，程序可能需要更长时间）。程序将报告算法在数据上的总运行时间。
2. 运行`pip install -r requirements.txt`下载必要的绘图包。接下来，尝试运行`python3 plot.py`，它将从运行`kmeans`生成的日志（"start.log"和"end.log"）生成文件"start.png"和"end.png"。这些文件将在当前目录中，应该类似于上图。（警告：你可能会注意到，并非所有点都分配给了“最近”的中心。这是可以的。）（对于那些想要了解原因的人：我们使用[PCA](https://en.wikipedia.org/wiki/Principal_component_analysis)将100维数据点降到2维，以生成这些可视化。因此，虽然100维数据点在高维空间中接近适当的中心，但数据点和中心的投影在2维中可能不接近彼此。只要聚类看起来“合理”（使用步骤2中的起始代码生成的图像作为参考），并且大多数点似乎分配给最近的中心，代码仍然是正确的。）
3. 利用`common/CycleTimer.h`中的计时函数确定代码中存在性能瓶颈的地方。你需要调用`CycleTimer::currentSeconds()`，它返回当前时间（以秒为单位）作为浮点数。代码的大部分时间花在哪里？
4. 根据上一步的发现，改进实现。我们期望实现约2.1倍或更高的加速（即$\frac{oldRuntime}{newRuntime} >= 2.1$）。请解释你是如何得出解决方案的，以及你的最终解决方案是什么以及相关的加速。这个过程的写作应该描述一系列步骤。我们期望的形式是“我测量了……这让我相信X。因此，为了改进，我尝试了……结果是加速/减速了……”。

约束：
- 你只能修改`kmeansThread.cpp`中的代码。你不能修改`stoppingConditionMet`函数，也不能更改`kMeansThread`的接口，但其他任何东西都是公平的（例如，你可以向`WorkerArgs`结构添加新成员，重写函数，分配新数组等）。然而……
- **确保你不改变实现的功能！如果算法不收敛或运行`python3 plot.py`生成的结果不像起始代码生成的那样，说明有问题！** 例如，你不能简单地删除主“while”循环或更改`dist`函数的语义，因为这会产生不正确的结果。
- __重要：__ 你只能并行化以下函数之一：`dist`、`computeAssignments`、`computeCentroids`、`computeCost`。有关如何使用`std::thread`编写并行代码的示例，请参见`prog1_mandelbrot_threads/mandelbrotThread.cpp`。

提示/注意事项：
- 这个问题不需要大量编码。我们的解决方案修改/添加了大约20-25行代码。
- 一旦你使用计时器隔离了热点，为了改进代码，请确保你了解K、M和N的相对大小。
- 尝试优先考虑具有高回报潜力的代码改进，并考虑问题中可用的不同并行轴以及如何利用它们。
- **这个程序的目的是让你更多地练习学习如何分析和调试性能导向的程序。即使你没有达到性能目标，如果你在写作中展示了良好/深思熟虑的调试技能，你仍然会获得大部分分数。**

## 关于ARM架构的Mac？##

对于那些可以访问新的Apple ARM架构笔记本电脑的人，尝试将ISPC编译目标更改为`ne### 程序3，第1部分。ISPC基础知识（20分中的10分）###

在阅读ISPC代码时，你必须记住，尽管代码看起来很像C/C++代码，但ISPC的执行模型与标准C/C++不同。与C不同，ISPC程序的多个程序实例总是在CPU的SIMD执行单元上并行执行。由编译器（并专门为底层机器选择）确定同时执行的程序实例数量。这个并发实例的数量可以通过内置变量`programCount`获得。ISPC代码可以通过内置的`programIndex`引用其自己的程序实例标识符。因此，从C代码调用ISPC函数可以被认为是生成了一组并发的ISPC程序实例（在ISPC文档中称为gang）。这组实例运行到完成，然后控制返回到调用的C代码。

__停下。这是你友好的讲师。请再次阅读前面的段落。相信我。__

例如，以下程序使用常规C代码和ISPC代码的组合来添加两个1024元素的向量。正如我们在课堂上讨论的那样，由于gang中的每个实例是独立的并且执行完全相同的程序逻辑，因此可以通过使用SIMD指令来加速执行。

下面是一个简单的ISPC程序。以下C代码将调用以下ISPC代码：

    ------------------------------------------------------------------------
    C程序代码：myprogram.cpp
    ------------------------------------------------------------------------
    const int TOTAL_VALUES = 1024;
    float a[TOTAL_VALUES];
    float b[TOTAL_VALUES];
    float c[TOTAL_VALUES];
 
    // 在这里初始化数组a和b。
     
    sum(TOTAL_VALUES, a, b, c);
 
    // 从sum返回后，a + b的结果存储在c中。

对应的ISPC代码：

    ------------------------------------------------------------------------
    ISPC代码：myprogram.ispc
    ------------------------------------------------------------------------
    export sum(uniform int N, uniform float* a, uniform float* b, uniform float* c)
    {
      // 假设programCount可以整除N。
      for (int i=0; i<N; i+=programCount)
      {
        c[programIndex + i] = a[programIndex + i] + b[programIndex + i];
      }
    }

上面的ISPC程序代码在程序实例之间交错处理数组元素。注意与程序1的相似之处，在程序1中你静态地将图像的部分分配给线程。

然而，与其考虑如何在程序实例之间划分工作（即，如何将工作映射到执行单元），通常更方便且更强大的是仅关注将问题划分为独立部分。ISPC的`foreach`构造提供了一种表达问题分解的机制。下面，ISPC函数`sum2`中的`foreach`循环定义了一个迭代空间，其中所有迭代都是独立的，因此可以以任何顺序执行。ISPC处理将循环迭代分配给并发程序实例。下面的`sum`和`sum2`之间的区别是微妙的，但非常重要。`sum`是命令式的：它描述了如何将工作映射到并发实例。下面的示例是声明式的：它仅指定要执行的工作集。

    -------------------------------------------------------------------------
    ISPC代码：
    -------------------------------------------------------------------------
    export sum2(uniform int N, uniform float* a, uniform float* b, uniform float* c)
    {
      foreach (i = 0 ... N)
      {
        c[i] = a[i] + b[i];
      }
    }

在继续之前，建议你通过阅读ISPC演练来熟悉ISPC语言结构，该演练可在<http://ispc.github.io/example.html>找到。演练中的示例程序几乎与程序3的`mandelbrot_ispc()`在`mandelbrot.ispc`中的实现完全相同。在作业代码中，我们更改了`foreach`循环的边界，以实现更直接的实现。

**你需要做的事情：**

1. 编译并运行程序mandelbrot ispc。__ISPC编译器当前配置为发出8宽AVX2矢量指令。__ 根据你对这些CPU的了解，你期望的最大加速是多少？为什么你观察到的数字可能低于这个理想值？（提示：考虑你正在执行的计算的特性？描述图像的哪些部分对SIMD执行提出了挑战？比较渲染Mandelbrot集的不同视图的性能可能有助于确认你的假设。）

我们提醒你，对于本节中描述的代码，ISPC编译器将程序实例的gang映射到单核上执行的SIMD指令。这种并行化方案与程序1不同，在程序1中，通过在多个核心上运行线程来实现加速。

如果你查看关于myth机器中CPU的详细技术资料，你会发现关于每个时钟周期可以运行多少标量和矢量指令有一套复杂的规则。对于本作业，你可以假设浮点数学的标量执行单元和8宽矢量执行单元的数量大致相同。

### 程序3，第2部分：ISPC任务（20分中的10分）###

ISPC的SPMD执行模型和`foreach`等机制促进了利用SIMD处理的程序的创建。该语言还提供了一种利用多个核心进行ISPC计算的附加机制。这种机制是启动_ISPC任务_。

参见函数`mandelbrot_ispc_withtasks`中的`launch[2]`命令。此命令启动两个任务。每个任务定义一个将由一组ISPC程序实例执行的计算。正如函数`mandelbrot_ispc_task`所示，每个任务计算最终图像的一个区域。类似于`foreach`构造定义了可以以任何顺序（并且由ISPC程序实例并行）执行的循环迭代，启动操作创建的任务可以以任何顺序（并且在不同的CPU核心上并行）处理。

**你需要做的事情：**

1. 使用参数`--tasks`运行`mandelbrot_ispc`。你在视图1上观察到的加速是多少？与不将计算划分为任务的`mandelbrot_ispc`版本相比，加速是多少？
2. 通过更改代码创建的任务数量，有一种简单的方法可以提高`mandelbrot_ispc --tasks`的性能。仅通过更改函数`mandelbrot_ispc_withtasks()`中的代码，你应该能够实现超过顺序版本代码32倍的性能。你是如何确定要创建的任务数量的？为什么你选择的数量效果最好？
3. _额外加分：（2分）_ 线程抽象（在程序1中使用）和ISPC任务抽象之间有什么区别？（创建/加入）和（启动/同步）机制之间在语义上的一些明显区别，但这些区别的影响更为微妙。这里有一个思维实验来指导你的答案：当你启动10,000个ISPC任务时会发生什么？当你启动10,000个线程时会发生什么？（对于这个思维实验，请在一般情况下讨论——即不要将你的讨论与给定的Mandelbrot程序联系起来。）

_聪明学生的问题_：嘿，等等！为什么ISPC系统有两种不同的机制（`foreach`和`launch`）来表达独立的、可并行化的工作？系统不能只将`foreach`的许多迭代划分到所有核心上并发出适当的SIMD代码吗？

_答案_：好问题！有很多可能的答案。来办公室时间讨论吧。

## 程序4：迭代`sqrt`（15分）##

程序4是一个ISPC程序，它计算2000万个0到3之间的随机数的平方根。它使用牛顿法的快速迭代实现来求解方程${\frac{1}{x^2}} - S = 0$。在此实现中，使用1.0作为初始猜测值。下图显示了`sqrt`在（0-3）范围内收敛到准确解所需的迭代次数。（对于此范围之外的输入，实施不会收敛）。请注意，收敛速度取决于初始猜测的准确性。

注意：这个问题是一个复习，以双重检查你的理解，因为它涵盖了与程序2和3相似的概念。

![sqrt收敛](handout-images/sqrt_graph.jpg "在0-3范围内以1.0为初始猜测的sqrt收敛。注意，对于输入值为1时，收敛是立即的，而当输入值接近0或3时，迭代次数增加（最高值为输入值为3时）。")

**你需要做的事情：**

1. 构建并运行`sqrt`。报告单个CPU核心（无任务）和使用所有核心（有任务）时的ISPC实现加速。SIMD并行化带来的加速是多少？多核并行化带来的加速是多少？
2. 修改数组values的内容，以提高ISPC实现的相对加速。构造一个特定的输入，__最大化相对于顺序版本代码的加速__，并报告所实现的加速（对于有任务和无任务的ISPC实现）。你的修改是否提高了SIMD加速？它是否提高了多核加速（即，从无任务的ISPC到有任务的ISPC的好处）？请解释原因。
3. 为`sqrt`构造一个特定的输入，__最小化ISPC（无任务）相对于顺序版本代码的加速__。描述这个输入，描述你选择它的原因，并报告ISPC实现的相对性能。效率损失的原因是什么？（请记住，我们使用`--target=avx2`选项为ISPC生成8宽SIMD指令）。
4. _额外加分：（最多2分）_ 使用AVX2内在函数手动编写`sqrt`函数的版本。要获得加分，你的实现应该几乎与使用ISPC生成的二进制文件一样快（或更快）。你可能会发现[Intel Intrinsics Guide](https://software.intel.com/sites/landingpage/IntrinsicsGuide/)非常有帮助。

## 程序5：BLAS `saxpy`（10分）##

程序5是BLAS（基本线性代数子程序）库中`saxpy`例程的实现，该库在许多系统上广泛使用（并经过高度优化）。`saxpy`计算简单操作`result = scale*X+Y`，其中`X`、`Y`和`result`是`N`个元素的向量（在程序5中，`N` = 2000万），`scale`是一个标量。注意，`saxpy`每使用三个元素执行两个数学操作（一个乘法，一个加法）。`saxpy`是一个*可平行化的计算*，具有可预测的、规则的数据访问和可预测的执行成本。

**你需要做的事情：**

1. 编译并运行`saxpy`。程序将报告`saxpy`的ISPC（无任务）和ISPC（有任务）实现的性能。你观察到的使用任务的ISPC加速是多少？解释该程序的性能。你认为它可以大幅改进吗？（例如，你能重写代码以实现接近线性加速吗？是或否？请解释你的答案。）
2. __额外加分：__（1分）注意，`main.cpp`中的总内存带宽消耗计算为`TOTAL_BYTES = 4 * N * sizeof(float);`。尽管`saxpy`从X加载一个元素，从Y加载一个元素，并将一个元素写入`result`，但乘以4是正确的。为什么会这样？（提示，考虑CPU缓存的工作原理。）
3. __额外加分：__（根据具体情况处理）提高`saxpy`的性能。我们在寻找显著的加速，而不仅仅是几个百分点。如果成功，请描述你是如何做到的，以及在这些系统上可能实现的最佳实现是什么。如果成功，请告诉工作人员，我们会感兴趣。;-)

注意：一些学生在这个问题上被困住了（想得太多）。我们期望一个简单的答案，但运行这个问题的结果可能会引发你脑中的更多问题。欢迎来与工作人员讨论。

## 程序6：加速`K-Means`（15分）##

程序6使用K-Means数据聚类算法（[维基百科](https://en.wikipedia.org/wiki/K-means_clustering)，[CS 221讲义](https://stanford.edu/~cpiech/cs221/handouts/kmeans.html)）对一百万个数据点进行聚类。如果你不熟悉该算法，不用担心！具体细节对练习并不重要，但在高层次上，给定K个起始点（聚类中心），该算法迭代更新中心，直到满足收敛标准。下图显示了程序开始和结束时算法的状态，其中红色星星是聚类中心，数据点颜色对应于聚类分配。

![K-Means起始和结束点](./handout-images/kmeans.jpg "K-Means算法应用于二维数据的起始和结束点。")

在起始代码中，你已经得到了K-means算法的正确实现，但在其当前状态下，它的速度还不够快。这就是你的任务！你的任务是找出实现需要改进的地方以及如何改进。这个问题的关键技能是__隔离性能热点__。我们不会告诉你在哪里查找代码。你需要自己找出来。你的第一个想法应该是……代码在哪里花费了最多的时间，你应该在源代码中插入计时代码进行测量。根据这些测量结果，你应该专注于占用大量运行时间的代码部分，然后更仔细地理解它，以确定是否有方法可以加速它。

**你需要做的事情：**

1. 使用命令`ln -s /afs/ir.stanford.edu/class/cs149/data/data.dat ./data.dat`在当前目录中创建一个指向数据集的符号链接（确保你在`prog6_kmeans`目录中）。这是一个大文件（约800MB），因此这是访问它的首选方式。然而，如果你想要一个本地副本，你可以在个人机器上运行此命令`scp [Your SUNetID]@myth[51-66].stanford.edu:/afs/ir.stanford.edu/class/cs149/data/data.dat ./data.dat`。一旦你有了数据，编译并运行`kmeans`（第一次尝试加载数据时，程序可能需要更长时间）。程序将报告算法在数据上的总运行时间。
2. 运行`pip install -r requirements.txt`下载必要的绘图包。接下来，尝试运行`python3 plot.py`，它将从运行`kmeans`生成的日志（"start.log"和"end.log"）生成文件"start.png"和"end.png"。这些文件将在当前目录中，应该类似于上图。（警告：你可能会注意到，并非所有点都分配给了“最近”的中心。这是可以的。）（对于那些想要了解原因的人：我们使用[PCA](https://en.wikipedia.org/wiki/Principal_component_analysis)将100维数据点降到2维，以生成这些可视化。因此，虽然100维数据点在高维空间中接近适当的中心，但数据点和中心的投影在2维中可能不接近彼此。只要聚类看起来“合理”（使用步骤2中的起始代码生成的图像作为参考），并且大多数点似乎分配给最近的中心，代码仍然是正确的。）
3. 利用`common/CycleTimer.h`中的计时函数确定代码中存在性能瓶颈的地方。你需要调用`CycleTimer::currentSeconds()`，它返回当前时间（以秒为单位）作为浮点数。代码的大部分时间花在哪里？
4. 根据上一步的发现，改进实现。我们期望实现约2.1倍或更高的加速（即$\frac{oldRuntime}{newRuntime} >= 2.1$）。请解释你是如何得出解决方案的，以及你的最终解决方案是什么以及相关的加速。这个过程的写作应该描述一系列步骤。我们期望的形式是“我测量了……这让我相信X。因此，为了改进，我尝试了……结果是加速/减速了……”。

约束：
- 你只能修改`kmeansThread.cpp`中的代码。你不能修改`stoppingConditionMet`函数，也不能更改`kMeansThread`的接口，但其他任何东西都是公平的（例如，你可以向`WorkerArgs`结构添加新成员，重写函数，分配新数组等）。然而……
- **确保你不改变实现的功能！如果算法不收敛或运行`python3 plot.py`生成的结果不像起始代码生成的那样，说明有问题！** 例如，你不能简单地删除主“while”循环或更改`dist`函数的语义，因为这会产生不正确的结果。
- __重要：__ 你只能并行化以下函数之一：`dist`、`computeAssignments`、`computeCentroids`、`computeCost`。有关如何使用`std::thread`编写并行代码的示例，请参见`prog1_mandelbrot_threads/mandelbrotThread.cpp`。

提示/注意事项：
- 这个问题不需要大量编码。我们的解决方案修改/添加了大约20-25行代码。
- 一旦你使用计时器隔离了热点，为了改进代码，请确保你了解K、M和N的相对大小。
- 尝试优先考虑具有高回报潜力的代码改进，并考虑问题中可用的不同并行轴以及如何利用它们。
- **这个程序的目的是让你更多地练习学习如何分析和调试性能导向的程序。即使你没有达到性能目标，如果你在写作中展示了良好/深思熟虑的调试技能，你仍然会获得大部分分数。**

## 关于ARM架构的Mac？##

如果您有新的苹果 ARM 笔记本电脑，请尝试将程序 3、4 和 5 中的 ISPC 编译目标更改为 neon-i32x8，编译档案更改为 aarch64。 其他程序只使用 GCC，应该能生成正确的目标。 在新的基于苹果 ARM 的笔记本电脑上生成各种程序的性能报告。 工作人员对您的发现很好奇。 您从 SIMD 执行中观察到了哪些加速？ 无法使用现代 Macbook 的人可以尝试使用 AWS 等云提供商提供的基于 ARM 的服务器，不过工作人员尚未对其进行过测试。 在提交作业之前，请确保将 ISPC 编译目标重设为 avx2-i32x8，并将编译架构重设为 x86-64，因为我们将在神话机器上测试您的解决方案！


## 对好奇者的推荐（强烈推荐） ##

想了解 ISPC 及其创建过程吗？ISPC 的两位创建者之一 Matt Pharr 写了一篇__精彩的博客文章__，讲述了其开发历史，名为 [The story of ispc](https://pharr.org/matt/blog/2018/04/30/ispc-all)。它触及了许多并行系统设计的问题——特别是有限范围与通用编程语言的价值。恕我直言，这是 CS149 学生必读的文章！

## 提交说明 ##

提交将通过 [Gradescope](https://www.gradescope.com) 进行。每组只需提交一次。但是，请确保在 Gradescope 提交中添加你伙伴的名字。你需要在 Gradescope 上的两个地方提交文件：`Assignment 1 (Write-Up)` 和 `Assignment 1 (Code)`。

请在 `Assignment 1 (Write-Up)` 中提交以下内容：
* 你的写作文件，命名为 `writeup.pdf`。请确保文档中包含两位组员的名字和 SUNet id（如果你是两人一组）。

请在 `Assignment 1 (Code)` 中提交以下内容：
* 你在程序 2 中实现的 `main.cpp`，文件名为 `prob2.cpp`
* 你在程序 6 中实现的 `kmeansThread.cpp`，文件名为 `prob6.cpp`
* 任何额外的代码，例如，你尝试了额外的加分项

请在写作文件中告知助教查找你的额外加分项。提交时，所有代码必须能够在 myth 机器上开箱即用地编译和运行！

## 资源和备注 ##

-  广泛的 ISPC 文档和示例可以在 <http://ispc.github.io/> 找到
-  放大曼德尔布罗特图像的不同位置非常有趣
-  英特尔提供了大量关于 AVX2 矢量指令的支持材料，网址为 <http://software.intel.com/en-us/avx/>
-  [Intel Intrinsics Guide](https://software.intel.com/sites/landingpage/IntrinsicsGuide/) 非常有用